import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.android.app.example',
  appName: 'CapacitorExampleApp',
  webDir: 'build'
};

export default config;
